# 发布流程

1. 改版本号：`gui.py` 与 `viewer_core.py` 里的 `APP_VERSION`
2. 自检：`py -3 viewer_core.py --selftest`、`py -3 viewer_meta.py`、`py -3 gui.py --selftest2`（真平台：设 `CV_PLATFORM=windows`）
3. 发版：双击 `发版.bat`（= `py -3 fab.py`）→ 产物在 `dist\Release\`（exe + 校验值.txt + 发行说明）
4. 打发布包：跑打包脚本生成 `CathayViewer 学术书库浏览与阅读 <版本>\` 并压 zip（含源码）
5. 上传：百度网盘（密码 2026）+ GitHub Release（附件用 ASCII 名）
6. 回填 README 下载表

## 坑

- `--name` 不要与源码包/目录同名
- PyQt6 用 PyInstaller 默认 hook 即可；PyMuPDF 会自带
- `.bat` 里不写中文（cmd 按 GBK 解析会乱）
