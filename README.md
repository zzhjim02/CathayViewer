<div align="center">

# 📚 CathayViewer

**学术书库浏览与阅读工具 —— 懂你的书库，读得舒服（只读）**

![license](https://img.shields.io/badge/license-GPL--3.0-blue)
![platform](https://img.shields.io/badge/platform-Windows%2010%2B-lightgrey)
![python](https://img.shields.io/badge/python-3.10%2B-3776ab)

</div>

## 🔗 Cathay 人文社科工具链

| 步骤 | 工具 | 功能 |
|:---:|---|---|
| ① | CathayIndex | 把本地文件夹建成可检索的「本地文件库」 |
| ② | CathayFinder | 综合性图书检索引擎：11 个渠道精准查书 |
| ③ | CathayPDG | 读秀/超星 PDG 批量转 PDF |
| ④ | CathayOCR | 扫描件 OCR，产出可搜索文字层 PDF |
| ⑤ | CathayShelf | 图书著录自动化整理 |
| ⑥ | **CathayViewer（本仓库）** | **书库浏览与阅读：文件名搜索、元数据、多版本、学术引用、阅读体验** |

> 🧭 CathayViewer 是 CathayReader 的升级版：把「本地书库」浏览、搜索、精读三件事做在一起。

## ✨ 功能

- **文件名索引**（不是全文索引）：扫指定书库目录建自己的索引库，增量刷新；**绝不改动源书库一个字节**
- **单窗口默认**：左侧「文件列表」+ 右侧「阅读器」，**分隔条可拖**、阅读器占约 3/4；**窗口大小/位置 + 分隔比例 + 是否拆窗都会记住**（默认 1200×820，不占满屏）；点「🗗 独立窗口」可把阅读器拆出去（关窗自动收回）　**（batch10）窗宽随意拉：PDF 会自动重新缩放，整页左右都看得见。**
- **列表 3 列**：序号 ｜ 文件名 ｜ 上级文件夹（只显示紧邻上一层；**单击文件名即打开**；命中关键词**黄底高亮**）
- **元数据解析**：从文件名取 书名 / 卷册 / 作者 / 出版社 / 年份 / SSID / 繁简标记（沿用 CathayShelf 规则）
- **多版本聚合**：同一本书的 `原本 PDF` / `_【繁转简】.txt` / `_PD6AIFOCR.pdf` 合成一条，下拉切换，原本排第一
- **一键复制学术引用**：`作者：《书名》，出版地：出版社，年份，第X页。`
- **阅读体验**：主题（浅色/深色/护眼）、字号 8–30、F11 全屏、**PDF 默认「适应页面」**、**顶部紧凑**（一行控件，详情默认隐藏、点「ⓘ」展开）、**PDF 连续纵向滚动阅读**（滚轮/触控板**像素级平滑滚动**，`↑`/`↓` 小步滚动，PgUp/PgDn 整页翻；页图懒渲染 + 缓存，同一页只渲染一次，不卡界面）、**缩放**（适应宽度 / 适应页面 / 100% + `Ctrl+滚轮`）、**可输入页码框 N / M**、**导航面板**（目录 + 缩略图 + 查找，打开 PDF/EPUB 默认展开、**目录单击即跳页**、自动高亮当前章节）、**文内查找**（Ctrl+F）、阅读位置记忆（Ctrl+B，重开自动跳回）
- **🗗 阅读器独立窗口**：一键把阅读区拆成独立窗口，最大化后几乎占满全屏看书（再点收回；关窗自动收回）
- **🖱 选中 PDF 里的字**：有文字层的 PDF 可直接鼠标拖选、**跨页连选**并复制（选完自动入剪贴板；右键「复制本页文字 / 全选本页」）
- **🔎 跨文件全文检索（在左侧列表上方）**：对**当前搜索结果**里的 **PDF 与对应 TXT** 逐页全文查找，**另开独立进程**跑，界面不卡；结果列表双击即跳到该页，可「全部复制」
- **检索去重（单文件 Ctrl+F 与跨文件共用一套）**：只在**同一目录**内、**只比对 PDF/TXT** ——
  · 同名 PDF 与 TXT（PDF 的 `_opt` 忽略、视为同名）：**命中数相同 → 只显示 PDF**；
  · 同名 PDF 与 TXT、**命中数不同 → 两条都显示，TXT 标「（TXT）」**；
  · 同一本书**不同 OCR 引擎**的 TXT（`_PD6AIFOCR`/`_PD6GJOCR`…）→ 都显示；
  · **原版 TXT 与对应繁/简 TXT** → 都显示（不互相顶掉）；无同名 PDF 的 TXT 照常显示。
  （命中数 = 关键词在该文件里的真实出现总次数；EPUB/MD/JSON 不参与配对）
- **被去重项默认隐藏**：单文件在「查找」页签底部、跨文件在结果窗底部，各有一个**「▸ 显示被去重的 N 项」小按钮**，点开可查看/复制（再点收起）。
- **📜 版权/授权信息**：选中文件时，从**文件名 + 上级目录名 + PDF 版权页（CIP）** 三处合并提取 授权 / 版权 / 出版发行 / ISBN / 版次 / 印次 / 出版年月，并标注每条来源
- **索引库位置自选**（可放移动硬盘/另一台机器），认盘用卷序列号，拔盘不崩
- **可选文件关联**：附 `关联.bat` / `取消关联.bat`（当前用户 HKCU，纯 ASCII），把 `.pdf/.epub/.txt/.md` 关联到 CathayViewer；**程序本体不碰注册表**，关联全靠你手动双击脚本
- **窗口自适应（batch10）**：PDF 缩放默认「适应页面」，窗口拉大/拉小会自动重排，**整页含左右边缘始终完整可见**（不会被导航栏挡住、也不留空）；文件列表**已去掉行号列**，文件名列自动铺满、**上级文件夹按内容显示全**；导航页签在窄窗口也不挤掉字；**版本下拉切换即真正打开该版本**；查找结果打不开时会明确提示（不再默默无反应）。

### 🆕 batch11 / batch12 新增

- **📷 截图本**：把当前 PDF 页面（或右键「▣ 框选截图」拖出的区域）一键存进**系统「文档」→ `Cathay文档记录\截图本\`**，保存时提示你输入页码；每张图在 `截图本.md` 里自动附**书名 / 页码 / 版本 / 文件**。
- **✂ 摘录本**：选中一段文字一键摘录，自动附**书名 · 卷册 · 页码 · 版本 · 出版社·年 · 文件名**，写到 **`文档\Cathay文档记录\摘录本\摘录本.md`**；摘录里若含纪年会**自动换算**（见下）。
- **⇄ PDF + TXT 对读**：打开 TXT 时若同目录有同名 PDF，**自动进入对读**；**PDF 在左、TXT 在右，中间是 PDF 导航页**，按页码标记**双向同步**翻页；进对读时 **PDF 停原页、TXT 同步到该页**；中间可直接切换 **TXT 版本**（不同 OCR / 繁简），并可一键 **只看 PDF / 只看 TXT**。
- **🕘 跨文件检索历史**：每次「跨文件全文检索」的结果**自动保存**，点「🕘 检索历史」随时调阅、双击重开该次结果、可删可清空。
- **⤒ 版权页更聪明**：打开版权页时**先查 PDF 目录（标签页/书签）里有没有「版权页 / 版权」**，没有才回退到读文字层。
- **🔗 相关文献四档**：Ctrl+R 把相关文件分成 **同丛书 / 同专题（同目录）/ 同一本书的其他书 / 同一作者的其他著作**。
- **❞ 注释一键插入（脚注）**：把选中引文（或剪贴板）+ 自动出处做成 **RTF 真脚注**，**粘到 Word 里即成脚注**（正文 + 脚注引用标记）。
- **⌛ 历史纪年换算**：识别引文中的**民国 / 年号 / 干支**纪年并在其后加【=公元年】（同时换算多个不同纪年）；**干支**列出 **1700–2000 年**全部对应年份。规则：**民国元年–民国38年不换算，民国39年及以后才自动换算**（如 民国四十年 = 1951 年）。并新增**随时可开的「历史纪年换算」小工具**（年号/民国/干支 ⇄ 公元互查）。**复制 PDF / TXT / 其它文件里的文字时**（含「复制本页文字」、鼠标拖选后右键复制）**自动在文末附上纪年换算**。

### 👤 人名别名归一（batch13）

近代人物常有多个 字 / 号 / 笔名 / 化名。搜「**吴佩孚**」会问你是否一并搜「子玉、吴玉帅、玉帅、孚威将军、孚威」；搜「**孙中山**」自动关联「孙文、逸仙、中山樵」。

- **内置一张可扩展的别名表**（含近代与古代人物，**随程序发布**，在 `config\person_alias.csv`，已达 **约 9.9 万人 / 15.7 万条**字号·笔名·化名）；检索到人物名时按设置处理：**询问我（默认）/ 自动并入 / 关闭**。命中的别名会一并进入结果，并在文件名里**多关键词黄底高亮**。
- **👤 人名别名**对话框（`Ctrl+Shift+A`）：查看/新增/删除、**导入/导出 CSV**、**从 CBDB 导入…**、按人名或字号反查。自己增补的写在本地 `person_alias.csv`（与设置同目录，不覆盖随包那份）。
- **数据来源**：内置精简表 + **CBDB（中国历代人物传记资料库）** 导出并入；也可继续导入你本地的 CBDB 官方导出目录（含 `ALTNAME_DATA.xlsx` / `BIOG_MAIN.xlsx`）——命令行 `py -3 开发\工具_CBDB导入别名表.py "<CBDB导出目录>"`（需 openpyxl）。同类数据源还有 Wikidata（别名，CC0）、中研院人名权威档、CN-DBpedia、维基/百科条目等。
- **致谢/许可**：别名数据含 **CBDB（Harvard/北大/中研院，学术免费、需引用）** 的衍生内容，请保留此出处。

### 🆕 batch14 改进与新增（2026-09-23）

- **⇄ 对读重做**：改为 **PDF（左）+ TXT（右）两纵列**，**中间一列是 PDF 导航**（页码 / 跳页 / ⏮◀▶⏭ / 同步开关 / 缩放）；不再一上一下。
- **对读可切 TXT 版本**：中间「TXT 版本」下拉列出同一本书的各个 TXT（不同 OCR / 繁简），随时切换。
- **对读可单独看**：中间底部「只看 PDF」「只看 TXT」直接退出对读，只留一侧（保持当前页）。
- **进对读保持页**：从 PDF 切到对读时，**PDF 停在第几页就停在那**，**TXT 跳到该页**。
- **左侧文件列表可缩很小**：最小宽度降到 90px，想看宽阅读区时随便拖。
- **「版本」下拉显示变宽**：最长 360px，长文件名不再被截断。
- **🗂 摘录本（查看 / 编辑器）**：新增「摘录本」对话框（`Ctrl+Shift+M` 或左栏按钮）——左侧列出全部摘录（可按书名/关键词过滤），右侧可**编辑正文与出处**，可**新增 / 删除 / 上移/下移 / 保存全部**；另一页签是「截图本」，可**预览并打开**历史截图。
- **⌛ 纪年换算工具**：现在 **民国 1–38 年也换算**（民国二十六年 = 1937）；**越界纪年**（如康熙63年）会**算出公元年并提示该年实际纪年**（康熙63年 = 1724 年 · 实为雍正二年）；**反查公元年时每个年号都注明是第几年**（1898 → 光绪二十四年、明治三十一年、光武二年）。

### 🆕 batch15 新增（2026-09-23）

- **拖入即打开**：直接把 **PDF / TXT**（也支持 .md/.epub/.json/.csv）**拖到窗口里**，即在阅读区打开；拖入多个只开第一个并提示其余已忽略。
- **双击用本软件打开**：仓仓根部的 `关联.bat`（用户级 / HKCU，不碰 HKLM、不装服务）现在会同时**把 PDF/TXT（及 EPUB/MD）设为默认打开方式**，双击即用本软件打开；`取消关联.bat` �同步骤回退。若 Windows 已记过默认项（UserChoice），可能需在「默认应用」里选一次。

### 🆕 batch16 性能修复（2026-09-23）

- **修复「双击文件列表很卡」**：深著录（读 PDF 文字层）以前在**主线程同步**做，部分 1–2 GB 的 PDF 要 **8–10 秒**（界面卡死）。现改为 **浅著录（毫秒级）立即显示 + 深著录后台线程补全 + 结果缓存**；重复选中同一文件**零成本**。
- **大书不再全文扫文字层**：页数 >200 的 PDF 只用前后采样（不逐页读全文），深著录从 8–10s 降到 **约 0.1–0.5s**。
- **超大 TXT 不再卡死**：普通阅读 / 对读最多载入 **前 8 MB**（带提示）；**>8 MB 的 TXT 不自动进对读**（可手动点「⇄ 对读」）。

### 🆕 batch17 界面（2026-09-23）

- **左侧文件列表可拖得极窄**（最小宽度 90 → **48 px**，可拖到 0 完全收起）：左栏顶部那排按钮（跨文件全文检索/检索历史/人名别名/摘录本）在左栏变窄时自动**收进「⋮」菜单**，不再把列表撑宽；再窄时自动收起「上级文件夹」列。
- **一键收起 / 展开左栏**：`Ctrl+Shift+L`。
- 「上级文件夹」列改为**可手动拖宽窄**（不再被长路径撑住）。

### 🆕 batch18 修复（2026-09-23）

- **独立阅读窗口也能用 `Ctrl+F`**（文内查找）：以前快捷键只挂在主窗口上，阅读器拆出后 Ctrl+F 无反应；现为独立窗口另挂窗口级 `Ctrl+F` / `Esc` / `F11`（该窗口全屏）。
- **最小化文件列表窗口不再连带最小化阅读器**：阅读器改为**独立顶层窗口**（无父窗口）。
- **直接打开文件时，左栏自动列出「同文件夹 + 文件名相似」的文件**（拖入 / 命令行 / 双击关联打开也会列出），并自动选中打开的那一个。

### 🆕 batch19 修复（2026-09-23）

- **`_PDV5AIFOCR` / `_PDV6AIFOCR` / `_PD5AIOCR` 等后缀以前归不到同一本**（会被当成「第X辑外交 PDV5」这种假书名）。现已修正：这类后缀与其他 OCR/繁简变体**聚成同一本书**，用「版本」下拉切换；PDF/TXT 也能对上。
- 另：CathayShelf 的 `core.py` 有**同一处**正则问题，已一并修好。

## ⌨️ 快捷键

| 快捷键 | 作用 |
|---|---|
| `F11` | 全屏（自动隐藏工具栏与按钮行，再按恢复） |
| `PgUp` / `PgDn` | PDF 上一页 / 下一页（跳页） |
| `↑` / `↓` | PDF 平滑小步滚动（滚轮同理） |
| `Ctrl+Home` / `Ctrl+End` | PDF 首页 / 末页 |
| `Ctrl+F` | 文内查找（回车查找；`Esc` 关闭查找条） |
| `Ctrl+滚轮` | PDF 缩放（配合「缩放」下拉：适应宽度 / 适应页面 / 100%） |
| `Ctrl+T` | 目录（导航面板「目录」页签；PDF / EPUB 大纲，没大纲就列页码；**单击一项即跳页**） |
| `Ctrl+E` | 用 PDF 引擎打开当前项（支持 EPUB，也可直接开 PDF） |
| `Ctrl+B` | 记住当前位置（同一文件下次打开自动跳回） |
| `Ctrl+H` | 阅读历史（双击重开） |
| `Ctrl+K` | 搜索历史 / 已保存的搜索（双击重搜，可删除） |
| `Ctrl+D` | 保存当前搜索关键词 |
| `Ctrl+Shift+H` | 阅读统计 |
| `Ctrl+M` | Markdown 渲染 ⇄ 源码 切换 |
| `Ctrl+J` | JSON 键值树（可展开、可过滤） |
| `Ctrl+R` | 相关文件（同丛书 / 同作者 / 相邻书架） |
| `Ctrl+Shift+T` | 缩略图（导航面板「缩略图」页签，PDF / EPUB） |
| `Ctrl+Shift+C` | 复制当前选中文本（**自动附历史纪年换算**） |
| `Ctrl+Shift+E` | ✂ 摘录选中文字到「摘录本」（自动附出处 + 纪年换算） |
| `Ctrl+Shift+X` | 📷 截图当前 PDF 页到「截图本」 |
| `Ctrl+Shift+D` | ⇄ PDF + TXT 对读（按页码同步） |
| `Ctrl+Shift+F` | 🕘 跨文件检索历史 |
| `Ctrl+Shift+I` | ❞ 注释一键插入（脚注，Word 可直贴） |
| `Ctrl+Shift+Y` | ⌛ 历史纪年换算工具 |
| `Ctrl+Shift+A` | 👤 人名别名表（字号/笔名/化名归一） |
| `Ctrl+Shift+M` | 🗂 摘录本（查看 / 编辑已摘录的资料） |

按钮：

- **⤒ 版权页**：跳到当前这本书的版权页（当前项是 txt 时自动找同书 PDF）
- **↗ 用外部程序打开**：用系统默认程序打开当前文件（**只有点它才会调用外部程序**）
- **🗗 独立窗口**：把阅读器拆成独立窗口 / 收回主窗口
- **🔎 跨文件全文检索（在左侧列表上方）**：对当前搜索结果做跨文件全文检索（独立进程），不卡界面
- **🕘 检索历史**：调阅历次跨文件全文检索结果（自动保存）
- **📷 截图** / **✂ 摘录** / **⇄ 对读** / **❞ 脚注** / **⌛ 纪年换算** / **👤 人名别名** / **🗂 摘录本**：见上方「batch11 / batch12 新增」「batch14 改进与新增」及「人名别名归一」

> 📖 **打开方式**：**单击文件名 / 双击 / 带文件启动，默认都在阅读器里打开**（不再甩给外部程序）；阅读区默认约占窗口 **3/4**。也可把 **PDF / TXT 直接拖进窗口**打开；`关联.bat` 可把 **PDF/TXT 设为双击默认用本软件打开**。
> 📜 **PDF 阅读**：连续纵向滚动，滚轮/触控板顺滑；当前页 ±2 预渲染、已渲染页缓存为 QPixmap（同一页只渲染一次）；缩放三档 + `Ctrl+滚轮`；页码框可直接输入 N / M 回车跳页；导航面板（目录 / 缩略图 / 查找）打开 PDF/EPUB 时默认展开。
> 🔍 **文内查找**：`Ctrl+F` 在当前**单个文件**内查找（不搜索引库）；TXT/MD/JSON/CSV 直接高亮循环，PDF 需有文字层（逐页搜索、页码+上下文列表、双击跳页并在页图高亮；无文字层会提示先做 OCR）。

## 🚀 怎么用

1. **首次打开**：向导三步 —— 选书库目录 → 选索引库位置 → 开始扫描（可暂停/停止）
2. 之后：搜索框输入书名/作者关键词 → 列表点一条 → 右侧看详情 → 点「📖 在阅读区打开」
3. 快捷键：见上面「⌨️ 快捷键」一节 —— `F11` 全屏 ｜ `PgUp/PgDn` 翻页 ｜ `Ctrl+T` 目录 ｜ `Ctrl+B` 记住位置 ｜ `Ctrl+H` 历史 ｜ `Ctrl+K` 搜索 …

## 📦 下载

| 方式 | 说明 |
|---|---|
| ✅ GitHub Releases | 见本仓库 Releases（Assets 直下 exe） |
| 📥 百度网盘（密码 2026） | 与本包同目录的 `校验值.txt` 一并附上；分享链接以发布时为准 |

包内包含：主程序 exe（单文件，自带 Python 环境）+ `config\`（出版社→城市表）+ 开发\（源码、打包脚本）。
包内**不含索引库**（`*.db`）；索引由你首次运行时自建。装包后可用同目录 `校验值.txt` 里的 SHA256 自检。

## 🔒 只读与安全

| 只做 | 绝不做 |
|---|---|
| 读源库的文件名/大小/时间 | 修改、改名、删除源库任何文件 |
| 写自己的索引库（你选位置） | 联网、装服务 |
| 记住阅读位置（写在程序旁设置文件） | 上传任何数据 |
| （可选）你用 `关联.bat` 手动建**当前用户**文件关联 | 程序本体**不碰注册表**、不动系统级 HKLM |

## 🔗 可选：文件关联（`.pdf/.epub/.txt/.md`）

包里有两个脚本（纯 ASCII，双击即可，无需管理员）：

- `关联.bat`：对**当前用户**（HKCU）写 ProgID `CathayViewer.<ext>` 与 `.ext\OpenWithProgids`；
  打开命令 = `"<exe完整路径>" "%1"`，图标指向 exe。exe 默认取脚本同级目录，
  若 exe 在别处，编辑脚本里的 `set "EXE=..."` 一行。
- `取消关联.bat`：反向 `reg delete /f` 撤销。

> ⚠️ **Windows 10/11 出于安全不允许程序直接抢默认打开方式**，所以注册后请再手动选一次：
> 「设置 → 应用 → 默认应用」里为该扩展名选 CathayViewer；或在文件上右键 →「打开方式」→
> 「选择其他应用」→ 勾选「始终使用此应用」。脚本不装服务、不改系统级 HKLM。

## 📝 更新日志

- **v0.1.0 · batch19**（2026-09-23）：**修复 `_PDV5AIFOCR`/`_PDV6AIFOCR` 等后缀归不到同一本**（后缀正则放宽为 `pd[a-z]{0,2}\d*`，族键里任意位置的 OCR 标记均剥）；CathayShelf 同一处问题一并修复。
- **v0.1.0 · batch18**（2026-09-23）：**独立窗口修复** —— 独立阅读窗口可用 `Ctrl+F` 查找（并支持 `Esc`/`F11`）；**最小化文件列表窗口不再带走阅读器**；**直接打开文件时左栏自动列出「同文件夹 + 相似文件名」**并选中它。
- **v0.1.0 · batch17**（2026-09-23）：**左侧文件列表可拖得极窄**（最小 48px / 可一键收起 `Ctrl+Shift+L`）；左栏按钮窄时自动收进「⋮」；「上级文件夹」列可手动拖宽窄。
- **v0.1.0 · batch16**（2026-09-23）：**修复双击列表卡顿** —— 深著录移到后台线程（浅著录秒回 + 缓存）、大书不全量扫文字层（深著录 8–10s → 约 0.1–0.5s）、超大 TXT 只载入前 8 MB（且不自动进对读）。
- **v0.1.0 · batch15**（2026-09-23）：**拖入 PDF/TXT 即打开**（多个只开首个）；`关联.bat`/`取消关联.bat` 支持把 **PDF/TXT（及 EPUB/MD）设为默认**打开方式。
- **v0.1.0 · batch14**（2026-09-23）：**对读重做**（左右两纵列、中间 PDF 导航、可切 TXT 版本、可只看 PDF/TXT、进对读保持页）；**左栏可缩很小**、**「版本」下拉变宽**；新增 **🗂 摘录本查看/编辑器**（`Ctrl+Shift+M`，含截图本预览页签）；**纪年换算工具支持民国 1–38 年**、**越界纪年提示实际纪年**、**反查注明年数**。
- **v0.1.0 · batch13**（2026-09-23）：**👤 人名别名归一** —— 检索人物名时可一并检索其 字/号/笔名/化名（询问 / 自动 / 关闭三模式，多关键词黄底高亮）；新增**人名别名表**对话框（增删/导入导出 CSV/从 CBDB 导入/反查）；内置别名表 **随包发布**（`config\person_alias.csv`，约 9.9 万人 / 15.7 万条，含 CBDB 衍生数据），并支持继续导入本地 **CBDB** 导出。
- **v0.1.0 · batch12**（2026-09-23）：**❞ 注释一键插入**（引文 + 出处 → RTF 真脚注，Word 可直贴）；**⌛ 历史纪年换算**（民国 / 年号 / 干支 → 公元，干支列出 1700–2000 全部年份；**民国38年内不换，39年起自动换**）；**复制文字（PDF/TXT/其它，含「复制本页文字」与拖选右键复制）自动附纪年换算**；**随时可开的「历史纪年换算」小工具**。
- **v0.1.0 · batch11**（2026-09-23）：**截图本 / 摘录本**（写到「文档\Cathay文档记录」，自动附出处）；**PDF + TXT 对读**（打开 TXT 有同名 PDF 则自动对读，按页码双向同步）；**跨文件检索历史自动保存**（🕘 可调阅）；**版权页先查 PDF 目录**；**相关文献四档**（同丛书/同专题/同一本书/同一作者）。
- **v0.1.0**（2026-09-22）**首版**：文件名索引 / 元数据著录 / 多版本聚合 / 学术引用 / 阅读体验（主题·字号·翻页·目录·记忆位置）/ 阅读统计 / 多格式（PDF·EPUB·TXT·MD·JSON）/ 相关文件 / 缩略图 / **PDF 连续滚动·导航面板·文内查找 / 可选文件关联脚本** / **单窗口+布局记忆 · 列表瘦身 3 列 · 跨页连选 · 检索纳入对应 TXT 并按规则去重 · PDF 默认适应页面 · 导航面板随阅读器 · 防双击重复打开**。

## 📄 许可

GPL-3.0。
