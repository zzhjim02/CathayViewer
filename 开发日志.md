# CathayViewer 开发日志

（Cathay 工具链浏览/阅读层 · PyQt6 · GPL-3.0 · 只读源库）

## 当前状态（2026-09-22）

| 文件 | 说明 | 验证 |
|---|---|---|
| `viewer_core.py` | 索引引擎：扫目录建库、增量刷新、原子替换、认盘（SN/UNC/根路径）、只读适配(foreign db)、FTS 搜索 | ✅ `py -3 viewer_core.py --selftest` → 13/13 OK；**源库共 mtime 逐一同（只读硬证明）** |
| `viewer_meta.py` | 元数据解析（书名/卷册/作者/出版社/年/SSID/繁简）、多版本聚合、学术引用、出版社→城市 | ✅ 9/9 OK；引用例：`王冠英：《中国伪书综考（全1册）》，合肥：黄山书社，1998年，第X页。` |
| `gui.py` | 首启向导(3页) + 主窗口：搜索/列表/预览/打开/版本下拉/📋复制引用/主题(浅色·深色·护眼)/字号8-30/F11全屏 | ✅ **真机启动正常**（窗口标题 `CathayViewer · 学术书库浏览与阅读 v0.1.0`）；分步日志回归全过 |

- `config\publishers.csv` 已自带（5122 B，从 CathayShelf 复制的只读参考）
- `开发\` 已建（放打包产物用）
- 沙箱：`D:\我的软件创作库\CathayViewer-DEV\` 已放行写入 ✓

## 已知问题（不影响产品）

1. `gui.py --selftest`（旧壳）与 `--selftest2`（新壳）都会在 **`MainWindow(st)` 构造处被原生层静默带走**（rc=1、无输出、faulthandler 也抓不到）。
   - 同一构造在真机启动（`main()`）与 `_gstep.py` 分步脚本里都正常 → **属测试上下文问题，非产品缺陷**。
   - 下一步：拿 `_gstep.py` 的构造方式逐行对照（它同进程内跑通）。
   - 临时验收替代：真机启动 + 进程存活 + 窗口标题 + 截图。
2. `--selftest2` 支持 `CV_PLATFORM=windows` 切真平台（默认 offscreen）。

## 下一步

1. ~M3 收尾：PDF 按页翻页渲染、目录树、阅读位置记忆~ ✅ 已完成（改为连续纵向滚动）
2. ~打包：PyInstaller onefile + 发布框架~ ✅ 已完成
3. ~M4 统计与推荐；M5 EPUB/JSON/MD 支持~ ✅ 已完成
4. ~修复自检壳~ ✅ 已完成（真因：QApplication 被 GC，非平台问题）

## 当前状态（2026-09-23 · batch16 后）

- **batch16**（修复「双击列表很卡」）：
  - 实测定位：`on_pick` 在 UI 线程同步 `META.parse(deep=True)`，大 PDF **10.7s**（全文扫文字层）；旧缓存只留一条。
  - **深著录入后台线程**：`MetaWorker(QThread)`；`on_pick` 先浅著录秒回 + `_render_info` 立即显示，深著录后台补全（`_request_deep_meta/_on_deep_meta`）；新增持久缓存 `_meta_map`。
  - **大书不全量扫**：`viewer_meta._pdf_text` 加 `_FULL_SCAN_MAX=200`（>200 页只前后采样），深著录 10.7s → 0.14s。
  - **大 TXT 不卡**：`TEXT_DISPLAY_MAX/DUAL_TXT_MAX = 8MB`（阅读/对读截断 + 提示）；>8MB TXT 不自动进对读。
  - 自检：`功能自检.py` **120/120 OK**（新增 118-120）。

- **batch15**（需求 10/11）：
  - **拖入即打开**：`MainWindow` 开 `setAcceptDrops(True)` + `dragEnterEvent/dragMoveEvent/dropEvent`（只接受文件且扩展名在 `READER_EXTS`）；拖入多个只开第一个并提示。
  - **抽取公用打开路径**：新增 `open_path_in_reader(p, note)`（选中 + 阅读区打开 + 提示），`open_cli_arg` 与拖放共用。
  - **双击关联（PDF/TXT）**：`关联.bat` / `取消关联.bat`（HKCU，ASCII，CRLF）新增 **设为默认** 行（`HKCU\Software\Classes\.pdf /ve` 等）—— 双击 PDF/TXT 即用本软件打开；Windows 已记 UserChoice 时可能需再选一次。
  - 自检：`功能自检.py` **117/117 OK**（新增 115-117）。

- **batch14**（需求 1–9 的改进与补缺）：
  - **⇄ 对读重做**（`DualRead`）：由「上下」改为 **左右两纵列（PDF 左 / TXT 右）+ 中间一列 PDF 导航页**（页码/跳页/⏮◀▶⏭/同步开关/缩放）。
  - **对读切 TXT 版本**：新增「TXT 版本」下拉（`_dual_txt_variants`，含繁简 / 不同 OCR），切换后重对齐当前页；新增信号 `txtChanged`。
  - **对读可单独看**：中间「只看 PDF / 只看 TXT」→ 信号 `exitRequested` → `_dual_single()` 退出对读、保持当前页。
  - **进对读保持页**（`_enter_dual`）：若当前读的就是这本 PDF，则 PDF 停原页、TXT 同步到该页（此前固定回第 1 页）。
  - **左栏可缩很小**：`tb.setMinimumWidth(90)`；**「版本」下拉变宽**：`cb_ver` 最小 160 / 最大 360。
  - **🗂 摘录本查看/编辑器**（`excerpt_viewer`，`Ctrl+Shift+M` + 左栏/按钮行按钮）：`viewer_tools` 新增 `parse_excerpts/build_excerpts/read_excerpts/write_excerpts/list_snapshots`；对话框两页签（摘录本可编辑：新增/删除/上移下移/保存全部；截图本可预览并打开）。
  - **⌛ 纪年换算（工具）**：`viewer_chrono` 新增 `allow_short_republic`——**工具里民国 1–38 年也换算**（复制/摘录自动注记仍不换算）；新增 `ERA_END` + 越界判定：**康熙63年 → 1724 年 + 提示「康熙共61年，无第63年；该年实为雍正二年」**；新增 `_cn_num/era_year_cn/eras_in_year/format_eras`，**反查公元年时每个年号都注明是第几年**（1898 → 光绪二十四年、明治三十一年、光武二年）。民国/令和仍在沿用，不做越界判定。
  - 自检：`功能自检.py` **114/114 OK**（新增 106-114）；`viewer_tools.py` / `viewer_chrono.py` / `gui.py --selftest(2)` 均 OK。

- **batch13**（需求 12 · 人名别名归一）：
  - 新增纯逻辑模块 `viewer_alias.py`：内置别名表（84 人/391 别名，含近代+古代）+ `lookup/expand/canonical_of`（正名↔字/号/笔名/化名）、`add/remove_person`、`import_csv/export_csv`、`import_cbdb_dir`（离线解析 CBDB 导出）。索引带缓存、`expand` 封顶 16（CBDB 同名号多）。
  - 主窗：`do_search` 先把关键词做别名归一（`_alias_extra`），命中的别名一并检索 + **多关键词黄底高亮**（`_HiDelegate` 改为多词）；新增 **👤 人名别名** 对话框（`Ctrl+Shift+A`，查看/增删/导入导出 CSV/从 CBDB 导入/反查）+ 左栏按钮；`st['alias_mode']` = ask（默认）/auto/off。
  - 数据源：本地 **CBDB** 官方导出（`ALTNAME_DATA.xlsx` + `BIOG_MAIN.xlsx`）；新增 CLI `工具_CBDB导入别名表.py`。**实测一套 20240820 导出 → 98,586 人 / 156,844 别名**（合并内置后 98,649 / 157,208），写入本地 `person_alias.csv`。
  - **发布**：用户要求**别名数据随软件发布** → 生成的别名表放到 `开发\config\person_alias.csv`（约 9.9 万人 / 15.7 万条），由 `make_release.py` 随 `config\` 一并打进发布包；模块启动时自动加载 bundled 表。`make_release.py` 仅排除**顶层**运行时用户表（`开发\person_alias.csv`），`config\` 下的随包发布。
  - **致谢/许可**：别名数据含 **CBDB（Harvard/北大/中研院，学术免费、需引用）** 的衍生内容，文档已标注出处。同类源：Wikidata（CC0）、中研院人名权威档、CN-DBpedia。
  - 自检：`功能自检.py` **105/105 OK**（新增 103-105）；`viewer_alias.py` 自检 12/12 OK。

## 当前状态（2026-09-23 · batch12 后）

- **batch11**（需求 1/2/3/7/8/9）：
  - 📷 **截图本**：当前 PDF 页（或右键▣框选区域）→ 系统「文档」\`Cathay文档记录\截图本\`，保存时提示页码；`截图本.md` 自动附出处。
  - ✂ **摘录本**：选中文字一键摘录 → `文档\Cathay文档记录\摘录本\摘录本.md`，自动附书名/卷册/页码/版本/出版信息。
  - ⇄ **PDF+TXT 对读**：新增 `TxtSyncView` + `DualRead`（PDF 上、TXT 下，按页码标记**双向同步**，`_guard` 防回环）；打开 TXT 遇同名 PDF **自动对读**（`st['auto_dual']` 默认 True）；版本切换不强制对读。
  - 🕘 **跨文件检索历史**：`_fts_done` 把结果存 `settings['fts_hist']`（按 kw 去重，最多 30）；新增「🕘 检索历史」对话框。
  - ⤒ **版权页先查目录**：`colophon_here` 先用 `get_toc()` 找「版权页/版权」，找不到再 `find_colophon_page`（文字层）。
  - 🔗 **相关文献四档**：`related_groups` 增 `series/topic/same_book/same_author`（保留 A/B/C 兼容）；对话框改四档。
- **batch12**（需求 10/11）：
  - ❞ **注释一键插入**：`TOOLS.footnote_rtf` 生成带 `\footnote` 的 RTF（汉字转 `\uN?`），复制到剪贴板 → Word 粘贴即成**真脚注**；HTML 作回退。
  - ⌛ **历史纪年换算**：新增 `viewer_chrono.py`（年号表 + 干支 60 循环）——民国/年号/干支→公元，干支列出 1700–2000 全部年份；**民国元年–38 年不换算，39 年起自动换算**；内置「历史纪年换算」小工具（换算 + 反查公元年）。
  - **复制自动换算**：`TextView`（文本）与 `PdfView.copy_selection` / 「复制本页文字」/ `copy_text` / `copy_html` 均在文末自动附【纪年换算】。
- 新增模块：`viewer_tools.py`（截图/摘录/对读页码索引 SyncIndex/相关推荐/检索历史/脚注 RTF）、`viewer_chrono.py`（纪年换算）。
- 自检：**`功能自检.py` 102/102 OK**；`viewer_core --selftest` / `viewer_meta` / `viewer_tools` / `viewer_chrono` / `gui.py --selftest` / `--selftest2` 全 OK。
- 新增写入位置仅限用户「文档」下自己的记录目录；**源书库仍一字节不改**。

## 当前状态（2026-09-23 · batch10 后）

- **batch9**：默认单窗口（左列表 ｜ 右阅读器，分隔条可拖、阅读器约 3/4）；记住窗口尺寸/位置 + 分隔比例 + 是否拆窗（默认 1200×820）；双击不再重复打开（防重入 + 元数据缓存）；导航面板跟随阅读器；单文件/跨文件检索都把对应 TXT 纳入范围并按规则去重；**被去重项默认隐藏 + 底部「▸ 显示被去重的 N 项」小按钮**。
- **batch10**：PDF 缩放**跟随窗口自动重排**（适应页面/适应宽度，整页左右始终可见、不被导航栏遮）；文件列表**去掉行号列**（序号只一列）、文件名列拉伸 + 上级文件夹列按内容显示全；导航页签不拉伸（窄窗口不挤掉字）；**版本下拉切换＝真正打开该版本**（以前只显示路径文字）；查找命中打不开时明确提示、不崩，且**打开 TXT 不再清空查找计数**；修掉打包版 `--fts-worker` 因 `sys.stdout=None` 崩溃。
- 自检：`功能自检.py` **91/91 OK**；`gui.py --selftest2` / `--selftest` / `viewer_core --selftest` / `viewer_meta` 全 OK。
- 发布包与源码对齐并剔除索引库 / 排障脚本。

## 当前状态（2026-09-22 深夜·batch9 后）

- **19/19 计划功能全部落地**，并在计划外增加：PDF 平滑滚动、缩放、Ctrl+F 文内查找、页码框、导航面板、文件关联脚本。
- **batch7**：PDF 专用内核平滑滚动 + 文字层选择/复制；阅读器可拆**独立窗口**；**跨文件全文检索**（独立进程）；目录单击即跳页；版权/授权三来源合并。
- **batch8**：列表瘦身 3 列 + 单击文件名即打开 + 关键词黄底；**跨页连选**；PDF 默认「适应页面」；顶部紧凑（89→29px）；检索按钮移到左栏。
- **batch9**：**默认单窗口**（左列表 ｜ 右阅读器，分隔条可拖、阅读器约 3/4）；**记住窗口尺寸/位置 + 分隔比例 + 是否拆窗**（默认 1200×820，不占满屏）；**双击不再重复打开**（防重入 + 元数据缓存）；**导航面板跟随阅读器**（拆窗时在阅读器右侧）；**单文件/跨文件检索都把对应 TXT 纳入范围**，并按去重规则展示（同命中数仅留 PDF / 不同则都留且 TXT 标「TXT」/ 不同 OCR 引擎都留 / 原版与繁简 TXT 都留，只限同目录、只比对 PDF/TXT、命中数按真实总次数）。
- 自检：`功能自检.py` **84/84 OK**；`gui.py --selftest2` / `--selftest` / `viewer_core --selftest` / `viewer_meta` 全 OK。
- **发布包与源码对齐**并**剔除索引库 / 排障脚本**。

## 硬约束（写死在代码与自检里）

- **绝不修改源书库**：只 `scandir`/读元数据，不建临时文件、不改名、不写一个字节
- 只写自己的索引库（位置由用户在向导里选，可放移动盘；主库 + 备查副本，卷序列号认盘）
- 不写注册表、不联网、不装服务
- 删除任何文件需用户明确确认
